server {
  listen 80;   # changed from 80  
  root /usr/share/nginx/html;

  # server_name 192.168.0.165;

  # ssl_certificate /etc/letsencrypt/live/192.168.0.165/fullchain.pem;
  # ssl_certificate_key /etc/letsencrypt/live/192.168.0.165/privkey.pem;

  # Allow only certain IPs
  # allow 192.168.0.0/24;   # local network
  # allow 203.0.113.45;     # your home/work IP
  # deny all;

  # root /var/www/app;
  # index index.html;

  location / {
    try_files $uri /index.html;
  }

  location /api {
    proxy_pass http://192.168.0.165:5400;
  }
}
