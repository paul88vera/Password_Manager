# # Redirect all HTTP to HTTPS
# server {
#     listen 80;
#     server_name server.verafied.tech;
#     return 301 https://$host$request_uri;
# }

# # HTTPS server block
# server {
#     listen 443 ssl;
#     server_name server.verafied.tech;

#     # SSL Certificates (same as backend)
#     ssl_certificate /etc/ssl/certs/app.verafied.tech.pem;
#     ssl_certificate_key /etc/ssl/certs/app.verafied.tech-key.pem;

#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;

#     root /usr/share/nginx/html;
#     index index.html;

#     # Serve static React build
#     location / {
#         try_files $uri /index.html;
#     }

#     # Optional — Proxy API calls to your backend server
#     location /api/ {
#         proxy_pass https://server.verafied.tech/api/;
#         proxy_http_version 1.1;

#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;

#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
